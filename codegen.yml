schema: http://localhost:8000/graphql
documents:
  - operation.graphql
generates:
  ./src/graphql.ts:
    config:
      withHooks: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
