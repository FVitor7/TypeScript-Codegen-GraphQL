schema: http://starlette-strawberry-graphql.herokuapp.com/graphql
documents:
  - operation.graphql
generates:
  ./src/graphql.ts:
    config:
      withHooks: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
